<Window
    x:Class="aoi_common.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cognex="clr-namespace:Cognex.VisionPro;assembly=Cognex.VisionPro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:aoi_common"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    Title="MainWindow"
    Width="800"
    Height="450"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Menu
            Grid.Row="0"
            Grid.ColumnSpan="2"
            VerticalAlignment="Center">
            <MenuItem Header="系统 (System)">
                <MenuItem Click="LoadJob_Click" Header="加载作业 (Load Job)" />
                <Separator />
                <MenuItem Click="Exit_Click" Header="退出 (Exit)" />
            </MenuItem>
            <MenuItem Header="视图 (View)">
                <MenuItem Click="OpenDebugView_Click" Header="打开调试窗口 (Open Debug View)" />
            </MenuItem>
            <MenuItem Header="控制 (Control)">
                <MenuItem Click="RunOnce_Click" Header="单次运行 (Run Once)" />
                <MenuItem Click="RunContinuous_Click" Header="连续运行 (Run Continuous)" />
            </MenuItem>
        </Menu>

        <WindowsFormsHost
            Grid.Row="1"
            Grid.Column="0"
            Margin="5">
            <cognex:CogRecordDisplay x:Name="mainRecordDisplay" />
        </WindowsFormsHost>
    </Grid>
</Window>
