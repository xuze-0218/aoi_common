<Window
    x:Class="aoi_common.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:aoi_common"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vpro="clr-namespace:Cognex.VisionPro;assembly=Cognex.VisionPro.Controls"
    Title="MainWindow"
    Width="1000"
    Height="450"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="配置">
                <MenuItem Command="{Binding OpenDebugCommand}" Header="程序调试" />
                <MenuItem Command="{Binding ParaDebugCommand}" Header="参数配置" />
                <MenuItem Command="{Binding CommunicateDebugCommand}" Header="通讯配置" />
            </MenuItem>
        </Menu>

        <StackPanel
            Grid.Row="1"
            Background="#F3F3F3"
            Orientation="Horizontal">
            <Button
                Width="80"
                Margin="5"
                Command="{Binding ImportImageCommand}"
                Content="导入图片" />
            <Button
                Width="80"
                Margin="5"
                Command="{Binding RunCommand}"
                Content="单次测试" />
            <TextBlock
                Margin="10,0"
                VerticalAlignment="Center"
                Text="{Binding Status}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="3" />
                <RowDefinition Height="150" MinHeight="100" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="250" />
                </Grid.ColumnDefinitions>

                <WindowsFormsHost Margin="2">
                    <vpro:CogRecordDisplay x:Name="mainRecordDisplay" />
                </WindowsFormsHost>

                <GroupBox
                    Grid.Column="1"
                    Margin="2"
                    Header="检测结果">
                    <StackPanel>
                        <TextBlock
                            Margin="5"
                            FontWeight="Bold"
                            Text="OK/NG 统计" />
                    </StackPanel>
                </GroupBox>
            </Grid>

            <GridSplitter
                Grid.Row="1"
                Height="3"
                HorizontalAlignment="Stretch"
                Background="#CCC" />

            <GroupBox
                Grid.Row="2"
                Margin="2"
                BorderBrush="#DDD"
                Header="系统日志">
                <DataGrid
                    AutoGenerateColumns="False"
                    Background="AliceBlue"
                    BorderThickness="0"
                    FontSize="12"
                    GridLinesVisibility="None"
                    IsReadOnly="True"
                    ItemsSource="{Binding LogSource}"
                    RowHeaderWidth="0">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding Timestamp}"
                            Header="时间">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="LightGray" />
                                    <Setter Property="Margin" Value="5,2" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn
                            Width="80"
                            Binding="{Binding Level}"
                            Header="级别">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{Binding Color}" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="HorizontalAlignment" Value="Center" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Message}"
                            Header="消息内容">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="5,2" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
