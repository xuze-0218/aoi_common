<Window
    x:Class="aoi_common.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:aoi_common"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    Title="MainWindow"
    Width="800"
    Height="450"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Background="#EEE" Orientation="Horizontal">
            <Button
                Width="100"
                Margin="10"
                Command="{Binding SwitchRunViewCommand}"
                CommandParameter="RunView"
                Content="切换运行界面" />
            <Button
                Width="100"
                Margin="10"
                Background="LightGreen"
                Command="{Binding RunVisionCommand}"
                Content="触发相机(Run)" />
        </StackPanel>
        <ContentControl
            Grid.Row="1"
            prism:RegionManager.RegionName="MainRegion"
            Background="Aqua" />
    </Grid>
</Window>
