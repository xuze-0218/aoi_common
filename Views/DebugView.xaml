<UserControl
    x:Class="aoi_common.Views.DebugView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cognex="clr-namespace:Cognex.VisionPro;assembly=Cognex.VisionPro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:aoi_common.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vproEdit="clr-namespace:Cognex.VisionPro.ToolBlock;assembly=Cognex.VisionPro.ToolGroup.Controls"
    d:DesignHeight="500"
    d:DesignWidth="1000"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <WindowsFormsHost>
            <vproEdit:CogToolBlockEditV2 x:Name="toolBlockEditV2" />
        </WindowsFormsHost>

        <Button
            Grid.Row="1"
            Width="100"
            Margin="5"
            HorizontalAlignment="Right"
            Command="{Binding CloseDialogCommand}"
            Content="确定并关闭" />

    </Grid>
</UserControl>
