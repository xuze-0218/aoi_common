<UserControl
    x:Class="aoi_common.Views.ParamConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:aoi_common.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="EnumComboStyle" TargetType="ComboBox">
            <Setter Property="ItemsSource" Value="{Binding DataContext.DataTypeValues, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        </Style>

        <Style x:Key="GroupItemStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander
                            Background="#F2F2F2"
                            BorderBrush="#CCCCCC"
                            BorderThickness="0,0,0,1"
                            IsExpanded="True">
                            <Expander.Header>
                                <StackPanel Margin="5,2" Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontWeight="Bold"
                                        Foreground="#555555"
                                        Text="模块: " />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontWeight="Bold"
                                        Foreground="#007ACC"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="Gray"
                                        Text="{Binding ItemCount, StringFormat=' ({0} 个参数)'}" />
                                </StackPanel>
                            </Expander.Header>
                            <ItemsPresenter />
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid MinHeight="200" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid
            HorizontalAlignment="Stretch"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ColumnWidth="120"
            ItemsSource="{Binding ParametersView}"
            SelectedItem="{Binding SelectedParameter}"
            SelectionMode="Single">

            <DataGrid.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource GroupItemStyle}" />
            </DataGrid.GroupStyle>

            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding ModuleName}"
                    Foreground="#007ACC"
                    Header="所属模块" />

                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding Name}"
                    Header="名称" />

                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Expression}"
                    Header="表达式" />

                <DataGridTextColumn
                    Width="200"
                    Binding="{Binding Note}"
                    Header="注释" />

                <DataGridTextColumn
                    Width="70"
                    Binding="{Binding InitValue}"
                    Header="初始值" />

                <DataGridComboBoxColumn
                    Width="90"
                    EditingElementStyle="{StaticResource EnumComboStyle}"
                    ElementStyle="{StaticResource EnumComboStyle}"
                    Header="输出类型"
                    SelectedItemBinding="{Binding OutputType}" />

                <DataGridTemplateColumn Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Background="Transparent"
                                BorderThickness="0"
                                Command="{Binding DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                Content="Del"
                                Foreground="Red" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="1"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="40"
                Margin="5,0"
                Command="{Binding AddCommand}"
                Content="+" />
            <Button
                Width="90"
                Margin="5,0"
                Background="#FF3C3C3C"
                Command="{Binding SaveCommand}"
                Content="保存配置"
                Foreground="White" />
        </StackPanel>
    </Grid>
</UserControl>